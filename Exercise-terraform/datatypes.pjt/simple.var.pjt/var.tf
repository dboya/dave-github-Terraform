/*     DATA Types:
- String (sequence of characters)= data type like sentence e.g. names such as private_subnet
- Numbers = pure numbers and fractions as well
- Maps = simply key-value pairs e.g. name = vpc
- List (sequence of values) = data type such as naming many different articles e.g. _AZ1 , AZ2 , AZ3 , AZ4, all of which constitue a single data block. List items are identified by whiole numbers stating with zero.
NB: Lists are represented in Square brackets */

/* (terraform-user = variable resource name). that is, anything thats found in that space is what i termed variable reource name. see its corresponding config file*/


variable "terraform-user" {

}
